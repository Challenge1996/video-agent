# 核心依赖
python-dotenv>=1.0.0
httpx>=0.25.0

# 视频处理
# ffmpeg 需要单独安装（通过系统包管理器）

# LangChain 和 LangGraph 用于 Agent
langchain>=0.1.0
langgraph>=0.0.20
langchain-core>=0.1.0

# TTS 服务（可选）
# google-cloud-text-to-speech>=5.0.0  # Google TTS 可选

# 测试
pytest>=7.0.0
pytest-asyncio>=0.21.0

# 工具
pydantic>=2.0.0
